spring:
  datasource:
    url:jdbc:mysql://localhost/db
    username:foo
    password:bar
    driver-class-name:com.mysql.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto:update
      
logging:
  level:
    app:DEBUG
